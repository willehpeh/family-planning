# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build family-planning`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t family-planning`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system family-planning && \
          adduser --system -G family-planning family-planning

COPY dist/apps/family-planning family-planning/
RUN chown -R family-planning:family-planning .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix family-planning --omit=dev -f install

CMD [ "node", "family-planning" ]
