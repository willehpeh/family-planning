# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build family-planning-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t family-planning-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system family-planning-api && \
          adduser --system -G family-planning-api family-planning-api

COPY dist/apps/family-planning-api family-planning-api/
RUN chown -R family-planning-api:family-planning-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix family-planning-api --omit=dev -f install

CMD [ "node", "family-planning-api" ]
